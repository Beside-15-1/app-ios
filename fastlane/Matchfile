git_url(ENV['GIT_URL'])

storage_mode("git")
git_branch("develop")
type("appstore")

app_identifier(ENV['APP_IDENTIFIER'])

username(ENV['APPLE_ID']) # Your Apple Developer Portal username
team_id(ENV['TEAM_ID'])

